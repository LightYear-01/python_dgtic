




def primo(n):
	for i in range(n-1,2)
		if (n % i == 0) && (n != i):
			return False
		else:
			return True


def primos(n):
	lista_primos = []
	done = False
	is_prime = 1

	while !done:
		if primo(is_prime):
			lista_primos.append(is_prime)
		is_prime++
		if lista_primos.count() == n:
			done = True

#Hacer una función que encuentre el palíndromo más
#grande contenido en una cadena
# Recibe: cadena a validar
# Regresa: cadena (palíndromo más grande)

#aqui falta hacerla recursiva